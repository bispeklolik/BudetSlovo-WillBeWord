name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      # Сборка + публикация в GitHub Release. Публикацию повторяем до 3 раз:
      # загрузка крупного .exe на uploads.github.com иногда срывается (флаки-сеть),
      # из-за чего релиз оставался без части ассетов (в т.ч. latest.yml).
      # Создаём релиз ЗАРАНЕЕ: иначе каждый ретрай electron-builder
      # создавал НОВЫЙ релиз на тот же тег, и /releases/latest мог указывать
      # на пустой дубль без установщика (ломая и скачивание, и автообновление).
      - name: Ensure single release for tag
        shell: pwsh
        run: |
          gh release view $env:GITHUB_REF_NAME 2>$null
          if ($LASTEXITCODE -ne 0) {
            gh release create $env:GITHUB_REF_NAME --title $env:GITHUB_REF_NAME --notes "Слово $env:GITHUB_REF_NAME"
          }
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and publish
        shell: pwsh
        run: |
          npm run build
          if ($LASTEXITCODE -ne 0) { throw "build failed" }
          for ($i = 1; $i -le 3; $i++) {
            npx electron-builder --publish always
            if ($LASTEXITCODE -eq 0) { break }
            if ($i -eq 3) { throw "publish failed after 3 attempts" }
            Write-Host "publish attempt $i failed, retrying in 20s..."
            Start-Sleep -Seconds 20
          }
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
